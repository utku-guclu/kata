#include <iostream>
#include <cstring>

void compress(char string[])
{
    for (int i = 0; i < strlen(string); i++)
    {
        std::cout << string[i] << "\n";
    }
    // char[] compressed = string.length();
    // return compressed;
}

int main()
{

    return 0;
}
